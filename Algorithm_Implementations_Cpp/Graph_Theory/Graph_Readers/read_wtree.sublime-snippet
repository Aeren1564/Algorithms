<snippet>
	<content><![CDATA[
int n;
cin >> n;
graph<int> g(n);
for(auto i = 0; i < n - 1; ++ i){
	int u, v, w;
	cin >> u >> v >> w, -- u, -- v;
	g.link(u, v, w);
}
]]></content>
	<!-- Optional: Set a tabTrigger to define how to trigger the snippet -->
	<tabTrigger>read_wtree</tabTrigger> -->
	<!-- Optional: Set a scope to limit where the snippet will trigger -->
	<scope>source.c++</scope> -->
</snippet>
