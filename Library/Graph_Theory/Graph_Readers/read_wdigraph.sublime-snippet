<snippet>
	<content><![CDATA[
int n, m;
cin >> n >> m;
graph<int> g(n);
for(auto i = 0; i < m; ++ i){
	int u, v, w;
	cin >> u >> v >> w, -- u, -- v;
	g.orient(u, v, w);
}
]]></content>
	<!-- Optional: Set a tabTrigger to define how to trigger the snippet -->
	<tabTrigger>read_wdigraph</tabTrigger> -->
	<!-- Optional: Set a scope to limit where the snippet will trigger -->
	<scope>source.c++</scope> -->
</snippet>
